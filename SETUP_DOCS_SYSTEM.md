# ドキュメント管理システムの構築指示

## あなたに依頼すること

このプロジェクトに「commitのたびにドキュメントを自動更新する仕組み」を構築してほしい。
以下の手順をすべて完了させること。

---

## Step 1: プロジェクトの調査

まず以下を調査して、後続のステップで使う情報を把握すること。

```
- コードベースの主要なドメイン（モデル・ストア・型定義など）を列挙する
- 主要なビジネスロジックがどこにあるか（routes/、stores/、lib/ など）を把握する
- 現在 CLAUDE.md があるか確認する
- package.json のスクリプト（test, build, lint など）を確認する
```

調査にはサブエージェントを使い、メインのコンテキストを汚染しないこと。

---

## Step 2: ディレクトリ構造の作成

以下の構造を作成すること。

```
docs/
├── domain/        # ドメイン仕様（状態・ビジネスルール・制約）
├── features/      # 機能仕様（処理フロー・条件・副作用）
├── adr/           # 意思決定の記録（背景・決定・理由）
└── index.md       # 全ドキュメントのインデックス（Claude・Obsidian共用）

.claude/
└── commands/
    └── commit.md  # /commit コマンド
```

---

## Step 3: テンプレートファイルの作成

### 3-1. docs/domain/_template.md

```markdown
# {{ドメイン名}}

## 概要
（1〜2行で何者か）

## 状態・ライフサイクル
（状態遷移があれば。なければ削除）

## ビジネスルール
- （条件、制約、例外。コードから自明なことは書かない）

## 操作と権限
- （誰が何をできるか）

## 関連機能
- [[features/xxx]] （この機能で影響を受ける）

## 調査起点
- `src/` 以下の関連ファイルパス
```

### 3-2. docs/features/_template.md

```markdown
# {{機能名}}

## 概要
（何をする機能か1行で）

## 処理フロー
1.
2.
3.

## 条件・制約
- 前提条件：
- 例外・エラーケース：

## 副作用
- （他ドメインへの影響、非同期処理など）

## 関連ドメイン
- [[domain/xxx]]

## 調査起点
- `src/` 以下の関連ファイルパス

## ADR
- （あれば [[adr/xxx]]）
```

### 3-3. docs/adr/_template.md

```markdown
# ADR-{{連番}}: {{タイトル}}

## ステータス
accepted / deprecated / superseded by [[adr/xxx]]

## 背景
（なぜこの決定が必要だったか）

## 決定
（何を決めたか）

## 理由
-
-

## 結果
- 良い面：
- トレードオフ：
```

---

## Step 4: docs/index.md の作成

Step 1 の調査結果をもとに、実際のドメインと機能を反映した index.md を作成すること。
テンプレートではなく、このプロジェクトの実態に即した内容にする。

```markdown
# ドキュメントインデックス

> このインデックスは調査の地図です。
> 正確性は保証されません。必ず実コードで確認してください。

## 使い方（Claude へ）
1. 該当するドメインまたは機能のファイルを Read ツールで開く
2. 「調査起点」に書かれたファイルを実際に読んで確認する
3. インデックスと実装に食い違いがあれば作業前に報告する

## ドメイン一覧
| ドメイン | 概要 | ファイル |
|---|---|---|
| （Step1で発見したドメインを記載） | | [[domain/xxx]] |

## 機能一覧
| 機能 | 概要 | ファイル |
|---|---|---|
| （Step1で発見した主要機能を記載） | | [[features/xxx]] |

## ADR一覧
| No. | タイトル | ステータス |
|---|---|---|
| （あれば記載、なければ「まだありません」） | | |

## 未ドキュメント化の領域
- （Step1で発見したが仕様書未作成のドメイン・機能を列挙）
```

---

## Step 5: 初期ドキュメントの生成

Step 1 の調査で把握したドメインのうち、**最も中心的なもの 2〜3 個**について
`docs/domain/` 以下にドキュメントを作成すること。

### 作成ルール
- テンプレートに沿って書く
- **コードから自明にわかること（型、メソッド名など）は書かない**
- コードだけでは分からないビジネスルールだけを書く
- 不明な箇所は `<!-- 要確認: xxx -->` と残す
- Obsidian の wikilink 形式 `[[ファイルパス]]` でリンクする（拡張子なし）
- 1ファイル 20 行以内を目安にする

機能仕様・ADR はこの時点では作らなくてよい。commitコマンドで育てていく。

---

## Step 6: CLAUDE.md の作成（または更新）

CLAUDE.md がなければ作成、あれば末尾に追記すること。

追記する内容：

```markdown
## ドキュメント管理

### 仕様書の使い方
docs/ 以下のファイルは「調査の地図」であり、正確性は保証されない。
作業前に必ず以下の順で動くこと：
1. docs/index.md で該当ドメイン・機能のファイルを特定する
2. そのファイルを Read ツールで開き、「調査起点」のコードを読む
3. 仕様書と実装に食い違いがあれば、作業を止めて報告する

### ドキュメント構成
- docs/domain/  : ドメイン仕様（状態・ルール）
- docs/features/: 機能仕様（処理フロー）
- docs/adr/     : 意思決定の記録
- docs/index.md : インデックス（必ずここから参照する）

### リンク形式
Obsidian の wikilink 形式を使う: [[domain/article]]（拡張子なし）
```

---

## Step 7: .claude/commands/commit.md の作成

以下の内容で作成すること。

````markdown
git add がされている変更をコミットし、必要であればドキュメントを更新する。

## 手順

### 1. 変更内容の把握
`git diff --staged --name-only` で変更ファイルを確認する。
変更がなければ「ステージングされた変更がありません」と伝えて終了する。

### 2. ドキュメント更新の要否判定

**更新不要（そのままコミットする）**
- リファクタリングのみ（処理内容が変わっていない）
- コメント・typo の修正
- テストの追加のみ（ビジネスルールの変更なし）
- スタイル・CSS の変更のみ

**更新が必要**
- ビジネスルール・処理条件の追加・変更
- 新機能の追加
- 状態遷移・権限の変更
- 他ドメインへの副作用が生じる変更

### 3. ドキュメントの更新

更新が必要と判断した場合、以下を行う。

**既存ドメイン・機能の変更の場合**
- docs/domain/ または docs/features/ の該当ファイルを更新する
- 更新内容：ビジネスルールと調査起点のみ。実装の詳細は書かない
- 仕様書と実装に矛盾がある場合：更新前に報告して確認を求める

**新規ドメインの場合**
- docs/domain/_template.md をコピーして新規ファイルを作成する
- docs/index.md のドメイン一覧に追記する

**新規機能の場合**
- docs/features/_template.md をコピーして新規ファイルを作成する
- 関連するドメインファイルの「関連機能」に wikilink を追記する
- docs/index.md の機能一覧に追記する

**ADR が必要な場合**（なぜそう実装したか説明が必要な設計判断）
- docs/adr/_template.md をコピーして連番で作成する
- 関連する機能ファイルの「ADR」セクションにリンクを追記する

### 4. コミットの実行

ドキュメントを更新した場合は、変更ファイルと一緒にステージングしてコミットする。

```
git add docs/
git commit -m "..."
```

コミットメッセージは Conventional Commits 形式で書く：
- feat: 新機能
- fix: バグ修正
- docs: ドキュメントのみの変更
- refactor: リファクタリング
- test: テストの追加・修正

### 5. 完了報告

以下を簡潔に伝える：
- コミットしたファイル
- ドキュメントを更新した場合はどのファイルを何の理由で更新したか
- ドキュメントを更新しなかった場合はその理由
````

---

## Step 8: 動作確認

以下を確認して報告すること。

```
1. docs/ の構造が正しく作成されているか
2. CLAUDE.md にドキュメント管理の記述が追加されているか
3. .claude/commands/commit.md が作成されているか
4. docs/index.md に実際のドメインが反映されているか
5. /commit コマンドが認識されるか（`ls .claude/commands/` で確認）
```

---

## 完了条件

以下がすべて満たされたら完了とする。

- [ ] docs/ ディレクトリ構造が存在する
- [ ] テンプレートファイルが 3 種類作成されている
- [ ] docs/index.md にプロジェクト実態が反映されている
- [ ] 中心的なドメインの仕様書が 2〜3 個作成されている
- [ ] CLAUDE.md にドキュメント管理の指針が記載されている
- [ ] .claude/commands/commit.md が作成されている

完了したら、各ファイルのパスと内容の概要を一覧で報告すること。
